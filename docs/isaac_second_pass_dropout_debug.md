# Isaac Second-Pass Dropout Debug Pack

This note tracks the seed-007 fused intermittent-anchor blocker before the
second-pass draft suite is rerun.

The debug pack lives under:

- `output/isaac_runs/latest_second_pass_dropout_debug/summary.csv`
- `output/isaac_runs/latest_second_pass_dropout_debug/summary.json`

The intended entrypoint is:

```bash
source .venv-isaac/bin/activate
export OMNI_KIT_ACCEPT_EULA=YES
python scripts/run_isaac_second_pass_dropout_debug.py --headless --execute-missing
```

Each run writes:

- `raw/dropout_debug_frames.csv`
- `raw/dropout_debug_events.csv`
- `analysis/dropout_debug_summary.json`
- the dropout timeline figures under `analysis/`

This file may be regenerated by the pack builder once real debug runs are on
disk. Until then, treat it as the checked-in landing page for the blocker.
