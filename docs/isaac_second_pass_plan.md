# Isaac Second-Pass Plan

The first Isaac pass is frozen on the `feature/isaac-runtime-first-pass` milestone and backed by the suite under `output/isaac_runs/latest_first_pass_suite`.

That suite is the immutable publication input for the first-pass paper milestone and must not be changed by later estimator work.

Future estimator-quality work continues on `feature/isaac-estimator-second-pass`.
